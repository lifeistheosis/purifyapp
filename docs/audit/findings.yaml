# Machine-readable findings index — principal audit 2026-07-11
# Frozen baseline: main @ dcd77c3. Full prose: docs/audit/2026-07-11-principal-audit.md
# status: confirmed | corrected-verified | corrected-unverified | accepted-risk
#         | requires-owner | requires-editorial-review | requires-store-review | mitigated
audit:
  date: 2026-07-11
  commit: dcd77c3
  branch: main

findings:
  - id: F-01
    title: Cancel/payment race can leave a paid order cancelled
    area: commerce
    severity: P1
    confidence: high
    status: corrected-verified   # 2026-07-11: lib/shop/webhookSettlement.ts recovery path (payment wins over cancellation); 11 unit tests incl. cancelled+completed->paid
    evidence: lib/shop/webhookSettlement.ts + lib/shop/__tests__/webhookSettlement.test.ts (11 passing)
    action: done; residual = live Stripe delivery unexercised (unit-level verification only)
    safe_to_automate: true
    review: none

  - id: F-02
    title: CI red for multiple releases (Node 20 vs node:sqlite); downstream gates dead
    area: release
    severity: P1
    confidence: confirmed
    status: corrected-verified   # CI run #317 on Node 24: lint/typecheck/unit/build all pass; pipeline alive again (fails later at pre-existing e2e stage, see F-15)
    evidence: .github/workflows/ci.yml:30 (was "20"); lib/content/* imports node:sqlite; owner-pasted failing run log
    action: bumped to Node 24 (C-01); owner pushes; consider gating Render on CI (owner decision)
    safe_to_automate: true
    review: none

  - id: F-03
    title: Stripe webhook does not verify amount/currency against the order
    area: commerce
    severity: P2
    confidence: confirmed
    status: corrected-verified   # 2026-07-11: amountsMatch guard, fail-closed; mismatch never marks paid; tested
    evidence: lib/shop/webhookSettlement.ts amountsMatch + tests (case-insensitive currency, null fails closed)
    action: done
    safe_to_automate: true
    review: none

  - id: F-04
    title: Baked commentary makes heaviest exported chapters ~1.3-3.3MB
    area: performance
    severity: P2
    confidence: confirmed
    status: confirmed
    evidence: out/bible/john/1 3289KB, genesis/1 1497KB, psalms/118 1317KB, matthew/5 1669KB (pre-existing pattern)
    action: measure on device first; if needed, ship commentary as per-chapter fetched JSON instead of baked HTML/flight
    safe_to_automate: false
    review: owner (design tradeoff)

  - id: F-05
    title: Victorinus on Revelation carries chiliast readings without doctrinal framing
    area: editorial
    severity: P2
    confidence: high
    status: deferred-by-owner   # 2026-07-11 owner decision: skip clergy review for now; queue retained
    evidence: data/bible/commentary/revelation/20.json (ANF07 recension)
    action: (deferred) editorial note on Rev 20 distinguishing early patristic opinion from Orthodox teaching
    safe_to_automate: false
    review: clergy/editorial

  - id: F-06
    title: Psalms verse-keying drift is quantified but unreviewed (170/2034 unmatched)
    area: editorial
    severity: P2
    confidence: confirmed
    status: confirmed
    evidence: ingest stats from scripts/ingest-augustine-psalms.mjs (1560 exact / 304 snapped / 170 unmatched)
    action: emit unmatched list to a review file on next ingest; editorial spot-check worst psalms
    safe_to_automate: partially
    review: editorial

  - id: F-07
    title: Reviews feature dark on prod until 20260711_shop_reviews.sql is applied
    area: database/release
    severity: P1-conditional
    confidence: confirmed
    status: resolved-verified   # 2026-07-11: service-role REST probes: shop_reviews 200 [], shop_products.units_sold 200, rpc shop_submit_review exists (403 'Sign in to review.' 28000)
    evidence: prod REST probes (table, column, RPC) + graceful API probes
    action: none; feature live pending buyer reviews
    safe_to_automate: false
    review: owner

  - id: F-08
    title: Sessionless order-cancel endpoint (deliberate; UUID + rate-limit gated)
    area: security
    severity: P2
    confidence: confirmed
    status: accepted-risk
    evidence: app/api/shop/checkout/cancel/route.ts
    action: none; residual risk documented (leaked link can cancel an unpaid checkout)
    safe_to_automate: n/a
    review: none

  - id: F-09
    title: No test coverage on API routes / money paths
    area: testing
    severity: P2
    confidence: confirmed
    status: confirmed   # C-02 added order-partition tests; routes still uncovered
    evidence: all 25 unit files target lib/**; none import app/api handlers
    action: mocked-Stripe webhook tests alongside F-01 fix; revive e2e via green CI
    safe_to_automate: true
    review: none

  - id: F-10
    title: Stale service-worker HTML after deploys can strand hydration-dependent pages
    area: reliability
    severity: P3
    confidence: strong-inference
    status: mitigated   # /account no-JS fallback shipped in 1.9.2; general case open
    evidence: public/sw.js NetworkFirst cached-HTML fallback vs hashed chunks; /account report signature
    action: consider version-change reload prompt; low urgency
    safe_to_automate: true
    review: none

  - id: F-11
    title: In-app Stripe checkout for physical goods needs Play-review confirmation
    area: store
    severity: P2
    confidence: policy-verified
    status: accepted-risk   # 2026-07-11: Play Payments policy (support.google.com/googleplay/android-developer/answer/10281818) explicitly EXCLUDES physical goods from Play Billing; alternative processors (Stripe) are the expected path. Residual: actual review acceptance of the build
    evidence: policy page fetched 2026-07-11; quoted exclusion of physical goods
    action: none beyond normal submission
    safe_to_automate: false
    review: store-policy

  - id: F-12
    title: Licensing posture record (no new risk found)
    area: legal
    severity: P3
    confidence: confirmed
    status: accepted-risk
    evidence: PD sources cited per note (NPNF/ANF/Brenton/KJV); supplier-image gate in lib/shop/catalog.ts; open owner items pre-existing (photos, LLC, sales tax, lawyer review)
    action: none new; 2026-07-11 owner decision: unique product photos when the physical batch arrives (skip for now)
    safe_to_automate: n/a
    review: owner (photos on batch arrival)

  - id: F-13
    title: supabase-js auth lock (navigator.locks) can hang client loads that await getUser()
    area: reliability/web
    severity: P1
    confidence: high
    status: partially-corrected   # 2026-07-11 product-page path guarded (hasActivePlusClient 2.5s fail-open race); other getUser()-gated client loads (orders, messages, requests, account gates) still hang-prone
    evidence: live walk 2026-07-11 signed-in browser: /shop/icons/* catalog+config APIs returned 200 (extension network log) while the page never left its skeleton; sole unsettled Promise.all member was hasActivePlusClient -> supabase.auth.getUser(); no console errors (silent lock wait). Matches the earlier '/account never loads' owner report signature.
    action: guarded the display-only Plus check (done); design per-surface handling for auth-required pages (a timeout there must NOT fake signed-out; show a retry state instead) — do not blanket-apply
    safe_to_automate: partially
    review: none

  - id: F-14
    title: Stripe return URLs derived from request origin resolve to localhost:10000 on prod
    area: commerce
    severity: P1
    confidence: confirmed-live
    status: corrected-verified-live   # re-walk on deployed code: Stripe back link lands on /shop/checkout/cancelled (real page), order row cancelled; residual: owner sets NEXT_PUBLIC_SITE_URL=https://purifyapp.net on Render (returns currently use the onrender fallback host)
    evidence: live cancel-path walk 2026-07-11 landed on https://localhost:10000/shop/checkout/cancelled?order=7085ab92... after Stripe's back link; Render proxy presents req.url as localhost:10000 (same hop documented in app/api/auth/signout). Affects SUCCESS path after real payments too (buyer strands on dead page; webhook still settles server-side).
    action: checkoutReturnOrigin() in lib/site.ts (SITE_URL except genuine local dev) + 4 unit tests; stranded walk order 7085ab92 cancelled via API and row verified cancelled/cancelled
    safe_to_automate: true
    review: none

  - id: F-15
    title: Playwright smoke + axe suite fails in CI (first signal since the gate revived)
    area: testing/accessibility
    severity: P2
    confidence: confirmed
    status: confirmed
    evidence: CI run #317 (23da374): lint/typecheck/unit/build green on Node 24; 'Run smoke suite + axe' step fails (runs ~5m30s total). Suite last executed weeks ago; failures are pre-existing e2e/a11y debt or stale specs, not a regression from 2026-07-11 work (all four runs #314-#317 fail at the same stage across unrelated commits).
    action: run test:e2e locally against a prod build, triage spec-by-spec (stale selectors vs real a11y violations), fix or quarantine with reasons; CI green end-to-end is the acceptance
    safe_to_automate: partially
    review: none

corrections:
  - id: C-01
    fixes: F-02
    files: [.github/workflows/ci.yml]
    verification: local suite green on Node 24 (254/254, tsc 0); CI run itself pending owner push
    status: corrected-unverified
  - id: C-02
    fixes: [F-09-partial]
    files: [lib/shop/orders.ts, lib/shop/__tests__/orders.test.ts, components/shop/OrdersClient.tsx]
    verification: vitest 254/254 incl. 5 new; tsc 0; eslint clean
    status: corrected-verified
