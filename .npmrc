# react-simple-maps@3 still declares a React <=18 peer range, but renders
# fine under React 19. Allow the install to proceed (matches local installs and
# unblocks the Render build, which otherwise fails npm install on ERESOLVE).
legacy-peer-deps=true
