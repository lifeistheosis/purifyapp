import { PrayerRope } from "@/components/prayers/PrayerRope";

export const metadata = {
  title: "Prayer rope",
  description:
    "A digital komvoschini — the prayer rope. Count the Jesus Prayer quietly. Local-first; signed-in users sync sessions across devices.",
};

export default function PrayerRopePage() {
  return <PrayerRope />;
}
